--- a/Makefile
+++ b/Makefile
@@ -86,6 +86,7 @@
 	cp sdl-config $(DESTDIR)$(PREFIX)/bin/sdl-config
 	
 install-headers:
+	mkdir $(DESTDIR)$(PREFIX)/include/SDL
 	cp include/*.h $(DESTDIR)$(PREFIX)/include/SDL
 
 install-lib:
